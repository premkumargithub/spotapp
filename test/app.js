'use strict';
var should = require('should'),
    chai = require('chai'),
    expect = chai.expect;
    
describe('App js file tests: ', function () {

    it('Should create namespace for the application', function (done) {
        done(); 
    });

});
